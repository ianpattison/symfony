{% if not post.tags.empty %}
    <p class="post-tags">
        {% for tag in post.tags %}
            <span class="label label-default">
                {{ tag.name }}
            </span>
        {% endfor %}
    </p>
{% endif %}

